{
  "name": "atlas-vendor-mock",
  "version": "0.1.0",
  "private": true,
  "description": "Mock vendor backend for /data-puller eval scenarios. Atlas talks to this; it returns canned tokens + alert data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cookie-parser": "^1.4.6"
  },
  "engines": {
    "node": ">=18"
  }
}
